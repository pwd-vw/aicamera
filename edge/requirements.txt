# AI Camera v2.0.0 Dependencies
# This file is a symlink to the unified requirements file
# The actual requirements are defined in: installation/requirements.txt

# For backward compatibility, this file now references the unified requirements
# To install dependencies, use: pip install -r installation/requirements.txt

# The unified requirements file includes:
# - Core Framework (Flask, SocketIO, etc.)
# - Camera and Image Processing (picamera2, OpenCV, etc.)
# - Database (SQLAlchemy, Alembic)
# - AI and OCR (EasyOCR, Degirum)
# - Communication (MQTT, SFTP, WebSocket)
# - Development and Testing tools
# - Simulation dependencies (optional)

# See installation/requirements.txt for the complete dependency list